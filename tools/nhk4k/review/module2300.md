# Review: module2300.cpp

Generated by upscale_wrap.py (dry-run). Flagged sites are NOT auto-wrapped (non-literal coordinate arguments = double-scaling risk). Fill in a decision for each.

Summary: 0 slot(s) already wrapped, 0 auto-wrappable literal(s), 0 flagged site(s).

No flagged sites.

## Human review (manual pass over the whole file)

Verdict: **module2300.cpp legitimately contains ZERO game-space coordinate
sites.** It is a module router around navigation scenes plus waterfall sound
management. No `module2300_sprites.*` files exist.

Reasoning, site by site:

- `createNavigationScene(0x004B67B8, which)` etc. - **don't scale.**
  Data-resource hashes + navigation index.
- `createSmackerScene(0x20080A0B, true, true, false)` - **don't scale.**
- `_vm->_soundMan->setSoundListParams(kModule2300SoundList, true, 50, 600,
  10, 150)`, `setSoundVolume(0x90F0D1C3, 15/87)` - **don't scale.** Sound
  volumes / timing parameters (`_waterfallSoundVolume = 15/87` are volumes).
- `createScene(N, M)` and `getNavigationIndex()` checks - **don't scale.**

Decision: no changes needed in this module. Coverage 100% by vacuity.
