# Review: module1800.cpp

Generated by upscale_wrap.py (dry-run). Flagged sites are NOT auto-wrapped (non-literal coordinate arguments = double-scaling risk). Fill in a decision for each.

Summary: 0 slot(s) already wrapped, 0 auto-wrappable literal(s), 0 flagged site(s).

No flagged sites.

## Human review (manual pass over the whole file)

Verdict: **module1800.cpp legitimately contains ZERO game-space coordinate
sites.** It is a pure module router: navigation scenes, smacker cutscenes and
the credits scene. No `module1800_sprites.*` files exist.

Reasoning, site by site:

- `createNavigationScene(0x004AFD38, which, kNavigationTypes00)` etc. -
  **don't scale.** Arg 1 is a data-resource hash, arg 2 a navigation index,
  arg 3 a byte table of navigation *types*
  (`static const byte kNavigationTypes00[] = {1, 0, 2, 0}`) - enums, not
  positions.
- `createSmackerScene(0x006C0085, true, true, false)` - **don't scale.**
- `new CreditsScene(_vm, this, false)` - **don't scale.** No coordinate
  parameters in the CreditsScene constructor.
- `createScene(4, 0)` / `leaveModule(2)` / `_moduleResult` comparisons -
  **don't scale.** Scene ids and results.
- `_vm->_soundMan->setSoundListParams(..., 50, 600, 10, 150)` - **don't
  scale.** SoundMan volume/countdown parameters; `600` looks coordinate-like
  but the identical call in fully covered module2200.cpp is left unscaled.

Decision: no changes needed in this module. Coverage 100% by vacuity.
