# Review: module1000.cpp

Generated by upscale_wrap.py (dry-run). Flagged sites are NOT auto-wrapped (non-literal coordinate arguments = double-scaling risk). Fill in a decision for each.

Summary: 32 slot(s) already wrapped, 0 auto-wrappable literal(s), 24 flagged site(s).

## line 168 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(UPSCALE(0, 0), tempSprite->getDrawRect().x2(), UPSCALE_Y(480));
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 172 - call-arg (setClipRect axis X, slot 2)

```cpp
_asDoor->setClipRect(UPSCALE(0, 0), tempSprite->getDrawRect().x2(), UPSCALE_Y(480));
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 266 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(UPSCALE(31, 0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart3->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 266 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(UPSCALE(31, 0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart3->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 274 - call-arg (setClipRect axis X, slot 0)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 274 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 274 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 282 - call-arg (setClipRect axis X, slot 0)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 282 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 282 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 290 - call-arg (setClipRect axis X, slot 0)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 290 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 290 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 299 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(UPSCALE(31, 0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart3->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 299 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(UPSCALE(31, 0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart3->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 310 - call-arg (tempClipRect.set axis X, slot 0)

```cpp
tempClipRect.set(tempSprite->getDrawRect().x, tempSprite->getDrawRect().y,
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 310 - call-arg (tempClipRect.set axis Y, slot 1)

```cpp
tempClipRect.set(tempSprite->getDrawRect().x, tempSprite->getDrawRect().y,
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 310 - call-arg (tempClipRect.set axis X, slot 2)

```cpp
tempClipRect.set(tempSprite->getDrawRect().x, tempSprite->getDrawRect().y,
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 311 - call-arg (tempClipRect.set axis Y, slot 3)

```cpp
_ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart2->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 343 - call-arg (setClipRect axis X, slot 0)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 343 - call-arg (setClipRect axis X, slot 2)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 343 - call-arg (setClipRect axis Y, slot 3)

```cpp
_klaymen->setClipRect(_ssLadderArch->getDrawRect().x, UPSCALE_Y(0), _ssLadderArchPart2->getDrawRect().x2(), _ssLadderArchPart1->getDrawRect().y2());
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 516 - call-arg (setClipRect axis Y, slot 1)

```cpp
_klaymen->setClipRect(UPSCALE_X(0), tempSprite->getDrawRect().y, UPSCALE(640, 480));
```

- Tool hint: runtime rect from another sprite - already in scaled space; do NOT wrap
- Decision: _(scale / don't scale / unsure - fill in)_

## line 613 - call-arg (drawString axis Y, slot 2)

```cpp
fontSurface->drawString(_background->getSurface(), UPSCALE_X(188), y, (const byte *)textStart);
```

- Tool hint: plain variable - trace its origin before scaling
- Decision: _(scale / don't scale / unsure - fill in)_

