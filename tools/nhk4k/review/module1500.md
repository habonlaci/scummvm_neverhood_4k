# Review: module1500.cpp

Generated by upscale_wrap.py (dry-run). Flagged sites are NOT auto-wrapped (non-literal coordinate arguments = double-scaling risk). Fill in a decision for each.

Summary: 0 slot(s) already wrapped, 0 auto-wrappable literal(s), 0 flagged site(s).

No flagged sites.

## Human review (manual pass over the whole file)

Verdict: **module1500.cpp legitimately contains ZERO game-space coordinate
sites.** Its 0 UPSCALE count does not mean missing coverage; there is nothing
to wrap. No `module1500_sprites.*` files exist.

Reasoning, site by site:

- `new Scene1501(_vm, this, 0x8420221D, 0xA61024C4, 150, 48)` (and the
  110/48 variants) - **don't scale.** The `Scene1501` constructor signature is
  `(vm, parentModule, uint32 backgroundFileHash, uint32 soundFileHash,
  int countdown2, int countdown3)`. `150/110/48` are tick countdowns
  (fade/skip timers used in `Scene1501::update()` against `_countdown1 = 12`),
  not pixels. Classic look-alike: integer pairs that resemble coordinates but
  are timers. The signature-driven matcher correctly proposed nothing here.
- `createScene(3, -1)` etc. - **don't scale.** Scene number + `which` entry.
- `createSmackerScene(0x001A0005, ...)`, `setBackground`, `setPalette`,
  `addBasePalette(hash, 0, 256, 0)`, `startFadeToPalette(12)`,
  `startFadeToBlack(11)` - **don't scale.** Hashes, palette indices/counts,
  fade durations.
- `sendMessage(_parentModule, 0x0800, 0)` - **don't scale.** Result code 0.

Decision: no changes needed in this module. Coverage 100% by vacuity.
